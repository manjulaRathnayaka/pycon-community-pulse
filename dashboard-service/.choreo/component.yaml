# +required The configuration file schema version
schemaVersion: 1.2

# +optional Incoming connection details for the component
endpoints:
  # +required Unique name for the endpoint
  - name: dashboard
    # +optional Display name for the endpoint
    displayName: PyCon Pulse Dashboard
    # +required Service section has the user service endpoint details
    service:
      # +optional Context (base path) of the API
      basePath: /
      # +required Numeric port value that gets exposed
      port: 8080
    # +required Type of traffic that the endpoint is accepting
    type: REST
    # +optional Network level visibilities of the endpoint
    networkVisibilities:
      - Public

# +optional Dependencies for the component
dependencies:
  serviceReferences:
    - name: Internal API Connection
      connectionConfig: 01f0a207-995d-188e-8ad6-35b2b449bf81
      env:
        - from: ServiceURL
          to: CHOREO_API_SERVICE_CONNECTION_SERVICEURL

# +optional Defines runtime configurations
configurations:
  # +optional List of environment variables
  env:
    - name: LOG_LEVEL
      valueFrom:
        configForm:
          displayName: Log Level
          required: false
          type: string
